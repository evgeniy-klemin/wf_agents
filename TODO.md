- [x] Для сабагента ревьювера разрешить git log (исправлено: splitBashCommands не разделял &&, поэтому pwd && git log не авто-апрувилось)
- [x] Для сабагента девелопера разрешить gofmt (добавлен авто-апрув в DEVELOPING/REVIEWING, deny в остальных фазах)
- [x] Либо увеличить кол-во итераций, либо подумать зачем они нужны (реализован soft limit: при достижении лимита спрашиваем юзера, reset-iterations сбрасывает счётчик)
- [x] Исправить вычисление общего счетчика итераций. Сейчас первая итерация в дашборде начинается с двойки.
- [x] Для сабагента девелопера авто-разрешить "go test ... | grep ..."
- [x] Добавить в инструкции Team Lead указание разбивать большие задачи на итерации по логическим блокам — коммитить и переходить в следующую итерацию, а не пытаться сделать всё за один проход.
- [x] После перехода в статус Completed не завершаются сами воркфлоу в Temporal.
- [x] Удалить или упростить PermissionRequest хендлер в hook-handler — для сабагентов он никогда не срабатывает (весь авто-апрув идёт через PreToolUse), для Team Lead избыточен после фикса guards.go.
- [x] Team Lxad забывает выполнить git checkout main перед созданием feature branch в PLANNING — усилить инструкции в feature-team-lead.md (сделать шаг branch setup более явным/императивным, добавить чеклист-формат).
- [x] Разобраться почему прибиваются агенты Developer и Reviewer в статусе Feedback. (Исправлено: SubagentStop больше не удаляет named teammates из activeAgents. Удаление только через agent-shut-down или clear-active-agents. E2e тесты подтверждают.)
- [ ] При старте всегда указывать параметр --task
- [x] Разрешите переход по флоу из ревьювинг в девелопинг.И убрать переход из ревьювинг в респаун.Для того, чтобы разработчик доделывал свою работу в текущем контексте из сессии. (Реализовано: REVIEWING→DEVELOPING напрямую, REVIEWING→RESPAWN убран. Обновлены guards, defaults.yaml, тесты, фронтенд, инструкции.)
- [ ] Доработать переход в статус блокинг.За основу можно взять оригинальный плагин. Поведением было удобное до текущего PR.Когда леду требуется какое-то действие от пользователя Он автоматом переводит это в блокинг.
- [x] Агенты, девелопер и ревьюер. Когда переходят в состояние идл, их удаляют из активных агентов Внутри Воркфлоу.И
  начинает отображаться ноль агентов.При этом, в клод-код сессии их видно. (Исправлено: SubagentStop для named teammates — no-op. Агенты остаются в activeAgents между Agent tool вызовами.)
- [ ] Доработать инструкцию по поводу вызов скриптов для анализа.А не подставлять один сессии в аргументы.сделать через локальный файл, который игнорируется в гите.Туда записывать аргументы.И файлы скриптов должны их подхватывать.Так не будет лишних запросов на опрув.
- [x] Исследовать, чтобы убрать регирование на команду агент стоп. Пологом видно, что они вызываются много раз. И
  реагирование на эти события приводят к некорректному статусу внутри Воркфлоу.Агенты на самом деле продолжают
  работать. (Исправлено: SubagentStop для named teammates игнорируется. Stop не влияет на activeAgents. Добавлен agent-shut-down сигнал для явного поштучного удаления.)
- [ ] Работать инструкции тем леда.Чтоб он не склеивал Команды для выполнения транзишена.Обязательные условия. Иначе он может скейпать инструкции от одного из транзишенов.
